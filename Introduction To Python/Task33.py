# (33)Write a Python program to check a list is empty or not.

l = []   # Define an empty list

if len(l) == 0:   # Check if the list is empty using len()
    print("list is empty")  # If the length is 0, the list is empty
else:
    print("list is not empty")   # If the length is greater than 0, the list has elements



