# (37)Write a Python program to convert a list of characters into a string. 

# Define a list of characters
l1 = ["D", "R", "A", "S", "H", "T", "I"]

# Use the join() method to convert the list into a single string
l2 = "".join(l1)  

# Print the string
print(l2)
