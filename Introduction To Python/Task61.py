# (61)Write a Python function to calculate the factorial of a number (anonnegative integer).

import math  # Import the math module

# Take user input 
x1 = int(input("Enter a number : "))

#using the built-in math function
x2 = math.factorial(x1)

# Return the result
x2
