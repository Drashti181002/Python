# (15) Write a Python program to calculate the length of a string. 


n = input("Enter a string: ")

print("Length of string is", len(n))   # Use the built-in len() function to calculate the length of the string

